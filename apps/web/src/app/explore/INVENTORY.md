# Inventory: explore

---

## page.tsx

### Imports

- `next/link` — `Link` component for client-side navigation
- `react-i18next` — `useTranslation` hook for i18n string lookup

### Definitions

- `ExplorePage` (component) — Client-side page component that renders the explore landing with links to `/explore/trips` and `/explore/groups`

### Exports

- `ExplorePage` — default
