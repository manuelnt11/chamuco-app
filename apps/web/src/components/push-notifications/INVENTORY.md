# Inventory: push-notifications

---

## PushNotificationsInit.tsx

### Imports

- `@/hooks/usePushNotifications` — `usePushNotifications` hook that handles FCM push notification setup

### Definitions

- `PushNotificationsInit` (component) — Client component that initializes push notification subscription by invoking `usePushNotifications`; renders nothing (returns `null`)

### Exports

- `PushNotificationsInit` — named
